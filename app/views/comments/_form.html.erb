
<div class="form-comment">
    <h4>Leave a comment</h4>
    <br>
     <%= simple_form_for([@shot, @shot.comments.build]) do |f| %>
        <%= f.input :name %>
        <%= f.input :response %>
        <%= f.button :submit, "Leave a reply", class:"btn btn-success" %>
    <%end%>

</div>